import PlaceholderPage from "@/components/dashboard/PlaceholderPage";
import { Users2 } from "lucide-react";

export default function CollaborationPage() {
  return (
    <PlaceholderPage
      title="Team Collaboration"
      subtitle="Discuss candidates and reach aligned hiring decisions"
      description="Compare candidates side by side, manage approval workflows, post team comments, and track where AI and human scores diverge — all in one shared workspace."
      icon={Users2}
    />
  );
}
